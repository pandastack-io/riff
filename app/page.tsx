import Landing from "@/components/landing/Landing";

// The home page is the landing page AND the product: the hero's composer is the
// real thing that creates a project and hands off to /project/[id].
export default function Home() {
  return <Landing />;
}
